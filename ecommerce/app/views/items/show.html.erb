<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @item.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @item.description %>
</p>

<p>
  <strong>Available:</strong>
  <%= @item.available %>
</p>

<%= link_to 'Edit', edit_item_path(@item) %> |
<%= link_to 'Add To Cart', add_to_cart_item_path(@item) if @item.cart_id.nil? %>
<%= link_to 'Back', items_path %>
